.direnv
.pio
.vscode/.browse.c_cpp.db*
.vscode/c_cpp_properties.json
.vscode/launch.json
.vscode/ipch
out/
# Verified flashable artifacts (see the dev notes "Build proof"). Kept on disk
# as the single trusted source to flash from, but not committed - they are ~1 MB
# binaries reproducible from the tagged source.
builds/
.direnv/
.DS_Store
.vscode/settings.json
.vscode/extensions.json
.idea
cmake-*
.cache
.ccls
compile_commands.json
.venv/
venv/
platformio.local.ini

# Local-only working docs (session memory, dev notes) - not published.
dev/
